services:
  dynamodb-local:
    image: amazon/dynamodb-local:latest
    container_name: sf-dynamodb
    user: root
    ports:
      - "127.0.0.1:${DYNAMO_PORT:-8010}:8000"
    command: >
      -jar DynamoDBLocal.jar -sharedDb -inMemory
    restart: unless-stopped
